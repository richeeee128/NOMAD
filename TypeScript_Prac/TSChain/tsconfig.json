{
  "include": ["src"],
  "compilerOptions": {
    "outDir": "build",
    "target": "ES6",
    // 타입스크립트가 어디에서 작동하는지 알려줌, DOM이 없다면 타입스크립트는 document 메서드를 읽지 못한다.
    "lib": ["ES6", "DOM"],
    "strict": true,
    "allowJs": true
  }
}
